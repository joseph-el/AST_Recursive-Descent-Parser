# include "Calculator.hpp"

int calculator(__unused int argc,__unused char *argv[] ) {
    string prompt;
    while (true) {
        int eof;
        _gnl("equation :> ", prompt);
        if (eof ^ -1)
            continue;
        

        Mgr.clearHeap();
    }

}